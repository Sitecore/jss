import { NAME } from './constants';

export const types = {
  INIT_STARTED: `${NAME}/INIT_STARTED`,
  INIT_COMPLETED: `${NAME}/INIT_COMPLETED`,
  CONTENT_REQUEST_STARTED: `${NAME}/CONTENT_REQUEST_STARTED`,
  CONTENT_REQUEST_COMPLETED: `${NAME}/CONTENT_REQUEST_COMPLETED`,
  CONTENT_REQUEST_FAILED: `${NAME}/CONTENT_REQUEST_FAILED`,
  VISIBILITY_TOGGLED: `${NAME}/VISIBILITY_TOGGLED`,
};
