const initialState = () => ({
  sitecore: {
    context: {
      pageEditing: false,
      language: 'en',
    },
    route: {
      placeholders: {},
    },
  },
  viewBag: {},
});

export default initialState;
