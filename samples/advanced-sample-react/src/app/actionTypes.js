import { NAME } from './constants';

export const types = {
  INIT_STARTED: `${NAME}/INIT_STARTED`,
  INIT_COMPLETED: `${NAME}/INIT_COMPLETED`,
  SUPPORTED_BROWSER_CHECK_COMPLETED: `${NAME}/SUPPORTED_BROWSER_CHECK_COMPLETED`,
};
