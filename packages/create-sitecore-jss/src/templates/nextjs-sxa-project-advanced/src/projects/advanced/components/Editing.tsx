/* eslint-disable @typescript-eslint/no-explicit-any */
const Editing = (props: any) => {
  return (
    <div>
      <h3>Editing Advanced component</h3>
      {JSON.stringify(props)}
    </div>
  );
};

export default Editing;
