import path from 'path';
import fs from 'fs';

const buildTemplates = () => {
  const cjsDistFolder = path.resolve(__dirname, 'dist', 'cjs');
  const esmdistFolder = path.resolve(__dirname, 'dist', 'esm');
}
